<!DOCTYPE html>
<html lang="en">
  <head>
    <% include ./partials/head %>
  </head>
  <body class="container">
    <header>
      <% include ./partials/header %>
    </header>
    <main>
      <div class="jumbotron">
        <h1><%= artwork.titleOne %></h1>
        <h2><%= artwork.artistTextOne %></h2>
        <img height="300" src='/img/full-res/<%= artwork.imageFilename %>' alt='<%= artwork.titleOne %>'>
        <div class "adv_nav">
          <a class "adv_nav_left" href='/'>Reject</a>
          <% if (index == 0) { %>
            <a class "adv_nav_right" href='/adventure/create/<%= artwork._id %>'>Approve</a>
          <% } else { %>
          <a class "adv_nav_right" href='/adventure/<%= adventure.id %>/add/<%= (parseInt(index) + 1) %>/<%= artwork._id %>'>Approve</a>
          <% } %>
        </div>
      </div>
    </main>
    <div id="background">
      <div id="fuzz"></div>
    </div>
  </body>
</html>
